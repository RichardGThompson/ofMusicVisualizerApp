#pragma once

namespace consts {
    const static int windowWidth = 1280;
    const static int windowHeight = 720;

    const string fontPath = "fonts/font.ttf";
    const string musicPath = "audio/song.mp3";
}